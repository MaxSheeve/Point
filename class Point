public class Point {
    private double x, y;
    Point (double x, double y){
        this.x = x;
        this.y = y;
    }
    public static double distance (Point p1, Point p2){
        double d =  Math.sqrt(Math.abs(p2.x - p1.x) + Math.abs(p2.y - p1.y));
        return d;
    }
}
